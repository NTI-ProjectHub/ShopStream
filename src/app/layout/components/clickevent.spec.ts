import { Clickevent } from './clickevent';

describe('Clickevent', () => {
  it('should create an instance', () => {
    const directive = new Clickevent();
    expect(directive).toBeTruthy();
  });
});
